# 项目接管说明

## 项目定位

`huifu` 是纯本地 Windows 多平台 AI 互动助手。通过连接安卓设备，在用户授权和平台实际能力范围内，完成关键词搜索、目标作品筛选、目标评论筛选、文字/图片/语音回复草稿及发布核验。

## 当前版本

`0.1.0-dev`

## 当前架构

- PySide6 原生桌面界面
- SQLite 本地数据库
- `%LOCALAPPDATA%\HuifuAI` 用户数据目录
- ADB 设备发现
- 四个平台统一适配器接口
- GitHub Actions Windows 自动测试和打包
- 打包后运行 `HuifuAI.exe --self-test`，启动入口或数据库初始化失败时禁止上传产物

## 已完成

- 主界面、首页统计、设备检测、数据库备份入口
- 第一版数据库结构和迁移
- 抖音、小红书、快手、哔哩哔哩插件注册表
- 文字、图片、语音、文字+图片回复能力模型
- ADB输出解析测试、数据库和备份测试

## 尚未完成

- 平台真实搜索、采集、登录检测和发布
- AI供应商配置页面和真实生成
- 业务画像、关键词和任务页面CRUD
- 图片模板和TTS语音
- ADB随安装包分发
- 安装程序和在线升级

## 不能破坏的规则

- 用户数据与程序目录分离。
- 更新前必须备份，更新不能删除现有数据。
- 平台密码不进入数据库。
- 未经真机验证的能力不得显示为可用。
- 发布结果不明确时不得自动重复提交。
- 不绕过验证码或平台安全验证。

## 下一阶段

1. 完成AI配置和业务画像CRUD。
2. 完成关键词导入及任务向导。
3. 接入ADB屏幕截图和UI结构采集。
4. 使用测试账号验证抖音搜索页面。
5. 实现抖音候选作品采集，不执行发布。

## 测试

```powershell
python -m pip install -e ".[dev]"
pytest
```

## 打包

```powershell
python -m pip install -e ".[build]"
pyinstaller --noconfirm --clean build/huifu.spec
```
